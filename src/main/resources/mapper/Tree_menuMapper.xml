<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.web.core.Mapper.Tree_menuMapper">

		<select id="selectAll"  resultType="tree_menu">
				select * from tree_menu where pid = 0
		</select>

		<select id="selectPid"  resultType="tree_menu">
				select * from tree_menu where pid=#{pid}
		</select>
	
</mapper>